# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
project './youtubedl-watchos.xcodeproj'

target 'youtubedl-watchos' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchTube

end

target 'youtubedl-watchos WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchTube for Apple Watch

end

target 'youtubedl-watchos WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchTube for Apple Watch Extension

  pod 'GoogleAPIClientForREST/YouTube'

end